const initState = {
    order:[]
}

export const orderData = (state=initState,action)=>{
     switch(action.type){
        default:
            return {
                ...state
            }
     }
}


